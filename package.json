{
  "name": "2025-a-420-1-c-1-ma",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "docusaurus": "docusaurus",
    "start:g2": "docusaurus start --config docusaurus.config.g2.js",
    "start:g3": "docusaurus start --config docusaurus.config.g3.js",
    "start:g4": "docusaurus start --config docusaurus.config.g4.js",
    "start:g5": "docusaurus start --config docusaurus.config.g5.js",
    "build:g2": "docusaurus build --config docusaurus.config.g2.js --out-dir build/g2",
    "build:g3": "docusaurus build --config docusaurus.config.g3.js --out-dir build/g3",
    "build:g4": "docusaurus build --config docusaurus.config.g4.js --out-dir build/g4",
    "build:g5": "docusaurus build --config docusaurus.config.g5.js --out-dir build/g5",
    "build:all": "npm run build:g2 && npm run build:g3 && npm run build:g4 && npm run build:g5",
    "swizzle": "docusaurus swizzle",
    "deploy": "docusaurus deploy",
    "clear": "docusaurus clear",
    "serve": "docusaurus serve",
    "write-translations": "docusaurus write-translations",
    "write-heading-ids": "docusaurus write-heading-ids"
  },
  "dependencies": {
    "@docusaurus/core": "3.8.1",
    "@docusaurus/preset-classic": "3.8.1",
    "@mdx-js/react": "^3.0.0",
    "clsx": "^2.0.0",
    "docusaurus-lunr-search": "^3.6.0",
    "prism-react-renderer": "^2.3.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@docusaurus/module-type-aliases": "3.8.1",
    "@docusaurus/types": "3.8.1"
  },
  "browserslist": {
    "production": [
      ">0.5%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 3 chrome version",
      "last 3 firefox version",
      "last 5 safari version"
    ]
  },
  "engines": {
    "node": ">=18.0"
  }
}
